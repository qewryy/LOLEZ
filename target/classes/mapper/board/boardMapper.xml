<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lolez.dao.BoardDao">

	<select id="selectBoardList" resultType="board">
		SELECT * FROM BOARDS
		WHERE BSTATE = '0' ORDER BY BDATE DESC
	</select>

	<select id="selectMaxBno" resultType="int">
		SELECT NVL( MAX(BNO),0 )
		FROM BOARDS
	</select>

	<insert id="insertBoard" parameterType="board">
		INSERT INTO BOARDS(BNO,
		BTITLE, BWRITER, BCONTENT, BDATE, BHITS, BREC,
		BSTATE )
		VALUES(#{bno},
		#{btitle}, #{bwriter}, #{bcontent}, SYSDATE, 0, 0, 0)
	</insert>

	<update id="updateBoardHits" parameterType="int">
		UPDATE BOARDS SET
		BHITS = BHITS + 1 WHERE BNO = #{viewBno}
	</update>

	<!-- <select id="selectBoardView" resultType="board">
		SELECT * FROM BOARDS WHERE BNO = #{viewBno}
	</select>

	<select id="selectLikeCheck" resultType="String">
		SELECT * FROM BOARDS WHERE LBNO = #{lbno} AND LMID = #{lmid}
	</select>
	
	<insert id="insertBoardLike" parameterType="board">
		INSERT INTO BOARDLIKE(LBNO, LMID, LSTATE) 
		VALUES(#{lbno}, #{lmid}, '0')
	</insert>
	
	<delete id="deleteBoardLike" parameterType="int">
		DELETE FROM BOARDLIKE 
		WHERE LBNO = #{lbno} AND LMID = #{lmid}
	</delete>
	
	<select id="selectBoardView" resultType="int">
		SELECT COUNT(*) FROM BOARDLIKE WHERE LBNO = #{lbno}
	</select>
	
	<select id="selectReplyLikeCount" resultType="int">
		SELECT COUNT(*) FROM REPLYLIKE WHERE RLNUM = #{renum}
	</select> -->
</mapper> 